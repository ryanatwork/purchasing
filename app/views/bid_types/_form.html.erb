<%= form_for(@bid_type) do |f| %>
  <% if @bid_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bid_type.errors.count, "error") %> prohibited this bid_type from being saved:</h2>

      <ul>
      <% @bid_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :abbreviation %><br />
    <%= f.text_field :abbreviation %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
